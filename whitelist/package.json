{
  "name": "WhiteList",
  "version": "0.0.0",
  "description": "White List testing binary search",
  "main": "whitelist.js",
  "scripts": {
    "test": "mocha --require should --reporter spec --ui bdd --recursive"
  },
  "author": "Scott G. Reynolds",
  "license": "ISC",
  "devDependencies": {
    "mocha": "*",
    "should": "*"
  },
  "dependencies": {
    "async": "^0.9.0"
  }
}
