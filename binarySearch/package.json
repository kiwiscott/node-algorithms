{
  "name": "BinarySearch",
  "version": "0.0.0",
  "description": "Binary Search CS Algo",
  "main": "binarySearch.js",
  "scripts": {
    "test": "mocha --require should --reporter spec --ui bdd --recursive"
  },
  "author": "Scott G. Reynolds",
  "license": "ISC",
  "devDependencies": {
   "mocha": "*",
   "should": "*"
 }
}
